export interface ComboType {
  name: string;
  combo: Array<number>;
  images: Array<string>;
  names: Array<string>;
  value: number;
  action_type: string;
  enforce_os_id: number;
  cost: number;
}
