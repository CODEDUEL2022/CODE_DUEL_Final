import React from 'react';
import { DeckCarouselPresentation } from './presentations/';

type Props = {};

export const DeckCarousel: React.FC<Props> = (props) => {
  return <DeckCarouselPresentation />;
};