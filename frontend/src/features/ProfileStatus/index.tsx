import React from 'react';
import { ProfileStatusPresentation } from './presentations/';

type Props = {};

export const ProfileStatus: React.FC<Props> = (props) => {
  return <ProfileStatusPresentation />;
};