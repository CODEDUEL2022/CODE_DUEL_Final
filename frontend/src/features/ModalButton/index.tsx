import React from 'react';
import { ModalButtonPresentation } from './presentations/';

type Props = {};

export const ModalButton: React.FC<Props> = (props) => {
  return <ModalButtonPresentation />;
};