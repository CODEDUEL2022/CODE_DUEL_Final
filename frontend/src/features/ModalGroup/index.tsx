import React from 'react';
import { ModalGroupPresentation } from './presentations/';

type Props = {};

export const ModalGroup: React.FC<Props> = (props) => {
  return <ModalGroupPresentation />;
};